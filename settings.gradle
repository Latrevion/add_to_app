pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_PROJECT)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "add_to_app"
include ':app'

setBinding(new Binding([gradle:this]))
evaluate(new File(
        settingsDir.parentFile,
        'flutter_add_to_native_app/.android/include_flutter.groovy'
))

include ':flutter_add_to_native_app'
project(':flutter_add_to_native_app').projectDir = new File('../flutter_add_to_native_app')

